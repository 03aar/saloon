# Base URL of the server/ API (see server/README.md for setup).
# Leave unset to run the frontend in demo mode: instant fake sign-in,
# no network calls, everything persisted to localStorage only.
# VITE_API_URL=http://localhost:4000
